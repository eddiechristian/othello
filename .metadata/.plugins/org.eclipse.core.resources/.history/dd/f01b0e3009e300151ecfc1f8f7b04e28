//use std::*;

#[derive(Debug)]
enum BoardSquareState {
	EMPTY,
	BLACK,
	WHITE
}


type ROW = Vec<BoardSquareState>;

struct OthelloBoard{
	rows: Vec<ROW>
}

fn main() {
	
	let n = 4;
	let size :usize = 2 * n;
	
	let rows :Vec<Vec<BoardSquareState>> = Vec::new();
	let mut board = OthelloBoard{ rows: vec![] };
	
    println!("row0 = {:?}",row0);
    println!("size = {:?}",size);
        
}
